// has to be manually synced with the build settings!!!

// TODO replace this with level id
// level ids continuous
// use separate class to translate between level id and build index

namespace SceneLoading {

    public enum SceneID {
        MovementTesting = 0,
        Test1 = 1,
        TerrainTest = 2
        // MAIN_MENU,
        // E1M1,
        
    }
}