using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour {

    void Start () {
        // Debug.Log(AxisConfig.GetLog());
        // if(!AxisConfig.TryLoadFromDisk()){  // all this should be done in some grand startup script. maybe even the input manager, since it's a monobehaviour
        //     AxisConfig.ResetToDefault();
        // }
        // Debug.Log(AxisConfig.GetLog());
        
        // Persistence.SaveFileHelper.DeleteAllData();
        // Persistence.FileHelper.OpenDataDirectory();
    }
	
}
